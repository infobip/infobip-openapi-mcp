spring:
  ai:
    mcp:
      server:
        enabled: true
        protocol: streamable
        type: sync
        stdio: false

infobip:
  openapi:
    mcp:
      security:
        auth:
          enabled: false

management:
  metrics:
    enable:
      all: false