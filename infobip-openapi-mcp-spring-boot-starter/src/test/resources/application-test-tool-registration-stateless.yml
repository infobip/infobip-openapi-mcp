spring:
  ai:
    mcp:
      server:
        enabled: true
        protocol: stateless
        type: sync
        stdio: false

infobip:
  openapi:
    mcp:
      security:
        auth:
          enabled: false

management:
  metrics:
    enable:
      all: false